name: Test Env Config

on:
   [ push, workflow_dispatch]

env:
    WORKFLOW_VARIABLE: DEV_ENVI

jobs:

    Development_Environment:
        runs-on: ubuntu-latest
        steps:
          - shell: pwsh
            env:
              SUPER_SECRET: ${{ secrets.GITHUB_TOKEN }}
            run: |
             echo ${{ secrets.GITHUB_TOKEN }}